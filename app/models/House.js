export default class House {
  constructor(address, color, rooms, price, imgUrl) {
    this.address = address
    this.color = color
    this.rooms = rooms
    this.price = price
    this.imgUrl = imgUrl
  }
}